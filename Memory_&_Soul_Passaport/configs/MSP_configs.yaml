# Memory & Soul Passport (MSP) Core Configuration (SSOT)
# Version: 8.1.0
# Date: 2026-01-03
# Description: Core orchestration parameters for the MSP Engine.

schema: EVA-MSP-Configs-v8.1
version: 8.1.0
updated: 2026-01-03

# ============================================================
# ENGINE SETTINGS
# ============================================================
engine:
  mode: "dual-phase" # "single-inference" | "async-persistence"
  enable_resonance_textures: true
  enable_trauma_logic: true
  max_retrieval_tokens: 4096

# ============================================================
# MEMORY ADMISSION POLICY
# ============================================================
admission:
  min_ri_score_to_index: 0.1
  force_index_tags: ["identity", "trauma", "learning"]
  compression_strategy: "semantic-summarization"

# ============================================================
# COMPONENT LINKS
# ============================================================
links:
  client: "Memory_&_Soul_Passaport/MSP_Client"
  rms: "Resonance_Memory_System"
  rag: "services/hept_stream_rag"
