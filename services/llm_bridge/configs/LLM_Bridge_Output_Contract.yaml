schema: EVA-LLM-Bridge-Output-Contract-v1
version: 1.0
updated: 2026-01-02

module: LLM Bridge
role: output_registry

destinations:
  - module: CIN
    contract_path: contract/downstream/CIN_Contract/
    output_fields:
      - raw_llm_response
      - function_calls

guarantees:
  - Output contains valid JSON function calls if requested
  - Context window limits are respected
